
Python ctypes bindings for VDIFIO

There is a script generate.py that requires python2 and in turn
requires the ctypes and ctypeslib packages, e.g., in Debian: 
aptitude install python-ctypes python-ctypeslib

python2 setup.py build


can be installed with

python setup.py install


